
            HBASE

适合存稀疏数据

满足范式纵切，数据量太大横切

AVLTree  完全平衡树

红黑树（看看）
B+树：一个叶子节点存一个链表（硬盘的必选树）

LSMTree 层级树

rocksdb（用rust写的）
HBASE存的是管理源数据的源数据

HBASE可以提前做几个分区，来缓解瞬间负载过大，导致服务器死

HBASE只有过滤器，没有查询器

Mysql查询方法：先建立一个搜索引擎，歌曲查出来的id（K）从库里面找数据V
Mysql适合批量查询，因为Mysql是树形结构

舆论算法：
       用udp进行广播实现elc之间的信息同步
索引方式：
      hash,B+,位图索引，k-d树，R树，倒排索引

倒排索引：至少需要三棵树，一个棵树记录的维度，第二棵树记录的是属性，第三个树建立的是文件名，第三个数是根据词频/逆频的大小排序的，越大月靠近根。
词频：集中出现在一个文件的
逆频：在多个文件上出现的
